# GraphHopper Directions API
#
# You use the GraphHopper Directions API to add route planning, navigation and route optimization to your software. E.g. the Routing API has turn instructions and elevation data and the Route Optimization API solves your logistic problems and supports various constraints like time window and capacity restrictions. Also it is possible to get all distances between all locations with our fast Matrix API.
#
# OpenAPI spec version: 1.0.0
# 
# Generated by: https://openapi-generator.tech


#' GHError Class
#'
#' @field code 
#' @field message 
#' @field hints 
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
GHError <- R6::R6Class(
  'GHError',
  public = list(
    `code` = NULL,
    `message` = NULL,
    `hints` = NULL,
    initialize = function(`code`, `message`, `hints`){
      if (!missing(`code`)) {
        stopifnot(is.numeric(`code`), length(`code`) == 1)
        self$`code` <- `code`
      }
      if (!missing(`message`)) {
        stopifnot(is.character(`message`), length(`message`) == 1)
        self$`message` <- `message`
      }
      if (!missing(`hints`)) {
        stopifnot(is.list(`hints`), length(`hints`) != 0)
        lapply(`hints`, function(x) stopifnot(R6::is.R6(x)))
        self$`hints` <- `hints`
      }
    },
    toJSON = function() {
      GHErrorObject <- list()
      if (!is.null(self$`code`)) {
        GHErrorObject[['code']] <- self$`code`
      }
      if (!is.null(self$`message`)) {
        GHErrorObject[['message']] <- self$`message`
      }
      if (!is.null(self$`hints`)) {
        GHErrorObject[['hints']] <- lapply(self$`hints`, function(x) x$toJSON())
      }

      GHErrorObject
    },
    fromJSON = function(GHErrorJson) {
      GHErrorObject <- jsonlite::fromJSON(GHErrorJson)
      if (!is.null(GHErrorObject$`code`)) {
        self$`code` <- GHErrorObject$`code`
      }
      if (!is.null(GHErrorObject$`message`)) {
        self$`message` <- GHErrorObject$`message`
      }
      if (!is.null(GHErrorObject$`hints`)) {
        self$`hints` <- lapply(GHErrorObject$`hints`, function(x) {
          hintsObject <- object$new()
          hintsObject$fromJSON(jsonlite::toJSON(x, auto_unbox = TRUE))
          hintsObject
        })
      }
    },
    toJSONString = function() {
       sprintf(
        '{
           "code": %d,
           "message": %s,
           "hints": [%s]
        }',
        self$`code`,
        self$`message`,
        lapply(self$`hints`, function(x) paste(x$toJSON(), sep=","))
      )
    },
    fromJSONString = function(GHErrorJson) {
      GHErrorObject <- jsonlite::fromJSON(GHErrorJson)
      self$`code` <- GHErrorObject$`code`
      self$`message` <- GHErrorObject$`message`
      self$`hints` <- lapply(GHErrorObject$`hints`, function(x) object$new()$fromJSON(jsonlite::toJSON(x, auto_unbox = TRUE)))
    }
  )
)
