/**
 * GraphHopper Directions API
 * You use the GraphHopper Directions API to add route planning, navigation and route optimization to your software. E.g. the Routing API has turn instructions and elevation data and the Route Optimization API solves your logistic problems and supports various constraints like time window and capacity restrictions. Also it is possible to get all distances between all locations with our fast Matrix API.
 *
 * OpenAPI spec version: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

#include "OAISolutionApi.h"
#include "OAIHelpers.h"
#include "OAIModelFactory.h"
#include "OAIQObjectWrapper.h"

#include <QJsonArray>
#include <QJsonDocument>

namespace OpenAPI {

OAISolutionApi::OAISolutionApi() {}

OAISolutionApi::~OAISolutionApi() {}

OAISolutionApi::OAISolutionApi(QString host, QString basePath) {
    this->host = host;
    this->basePath = basePath;
}

void
OAISolutionApi::getSolution(QString* key, QString* job_id) {
    QString fullPath;
    fullPath.append(this->host).append(this->basePath).append("/vrp/solution/{jobId}");

    QString job_idPathParam("{"); job_idPathParam.append("jobId").append("}");
    fullPath.replace(job_idPathParam, stringValue(job_id));

    if (fullPath.indexOf("?") > 0)
      fullPath.append("&");
    else
      fullPath.append("?");
    fullPath.append(QUrl::toPercentEncoding("key"))
        .append("=")
        .append(QUrl::toPercentEncoding(stringValue(key)));


    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker();
    OAIHttpRequestInput input(fullPath, "GET");





    foreach(QString key, this->defaultHeaders.keys()) {
        input.headers.insert(key, this->defaultHeaders.value(key));
    }

    connect(worker,
            &OAIHttpRequestWorker::on_execution_finished,
            this,
            &OAISolutionApi::getSolutionCallback);

    worker->execute(&input);
}

void
OAISolutionApi::getSolutionCallback(OAIHttpRequestWorker * worker) {
    QString msg;
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type == QNetworkReply::NoError) {
        msg = QString("Success! %1 bytes").arg(worker->response.length());
    }
    else {
        msg = "Error: " + worker->error_str;
    }

    QString json(worker->response);
    OAIResponse* output = static_cast<OAIResponse*>(create(json, QString("OAIResponse")));
    auto wrapper = new OAIQObjectWrapper<OAIResponse*> (output);
    wrapper->deleteLater();
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        emit getSolutionSignal(output);
    } else {
        emit getSolutionSignalE(output, error_type, error_str);
        emit getSolutionSignalEFull(worker, error_type, error_str);
    }
}


}
