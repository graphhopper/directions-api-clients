# GraphHopper Directions API
#
# You use the GraphHopper Directions API to add route planning, navigation and route optimization to your software. E.g. the Routing API has turn instructions and elevation data and the Route Optimization API solves your logistic problems and supports various constraints like time window and capacity restrictions. Also it is possible to get all distances between all locations with our fast Matrix API.
#
# OpenAPI spec version: 1.0.0
# 
# Generated by: https://openapi-generator.tech

#' @title Routing operations
#' @description GraphHopper.Routing
#'
#' @field path Stores url path of the request.
#' @field apiClient Handles the client-server communication.
#' @field userAgent Set the user agent of the request.
#'
#' @importFrom R6 R6Class
#'
#' @section Methods:
#' \describe{
#'
#' route_get Routing Request
#'
#' }
#'
#' @export
RoutingApi <- R6::R6Class(
  'RoutingApi',
  public = list(
    userAgent = "OpenAPI-Generator/1.0.0/r",
    apiClient = NULL,
    initialize = function(apiClient){
      if (!missing(apiClient)) {
        self$apiClient <- apiClient
      }
      else {
        self$apiClient <- ApiClient$new()
      }
    },
    route_get = function(point, points_encoded, key, locale, instructions, vehicle, elevation, calc_points, point_hint, ch_disable, weighting, edge_traversal, algorithm, heading, heading_penalty, pass_through, details, round_trip_distance, round_trip_seed, alternative_route_max_paths, alternative_route_max_weight_factor, alternative_route_max_share_factor, avoid, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`point`)) {
        queryParams['point'] <- point
      }

      if (!missing(`locale`)) {
        queryParams['locale'] <- locale
      }

      if (!missing(`instructions`)) {
        queryParams['instructions'] <- instructions
      }

      if (!missing(`vehicle`)) {
        queryParams['vehicle'] <- vehicle
      }

      if (!missing(`elevation`)) {
        queryParams['elevation'] <- elevation
      }

      if (!missing(`points_encoded`)) {
        queryParams['points_encoded'] <- points_encoded
      }

      if (!missing(`calc_points`)) {
        queryParams['calc_points'] <- calc_points
      }

      if (!missing(`point_hint`)) {
        queryParams['point_hint'] <- point_hint
      }

      if (!missing(`ch_disable`)) {
        queryParams['ch.disable'] <- ch_disable
      }

      if (!missing(`weighting`)) {
        queryParams['weighting'] <- weighting
      }

      if (!missing(`edge_traversal`)) {
        queryParams['edge_traversal'] <- edge_traversal
      }

      if (!missing(`algorithm`)) {
        queryParams['algorithm'] <- algorithm
      }

      if (!missing(`heading`)) {
        queryParams['heading'] <- heading
      }

      if (!missing(`heading_penalty`)) {
        queryParams['heading_penalty'] <- heading_penalty
      }

      if (!missing(`pass_through`)) {
        queryParams['pass_through'] <- pass_through
      }

      if (!missing(`details`)) {
        queryParams['details'] <- details
      }

      if (!missing(`round_trip_distance`)) {
        queryParams['round_trip.distance'] <- round_trip_distance
      }

      if (!missing(`round_trip_seed`)) {
        queryParams['round_trip.seed'] <- round_trip_seed
      }

      if (!missing(`alternative_route_max_paths`)) {
        queryParams['alternative_route.max_paths'] <- alternative_route_max_paths
      }

      if (!missing(`alternative_route_max_weight_factor`)) {
        queryParams['alternative_route.max_weight_factor'] <- alternative_route_max_weight_factor
      }

      if (!missing(`alternative_route_max_share_factor`)) {
        queryParams['alternative_route.max_share_factor'] <- alternative_route_max_share_factor
      }

      if (!missing(`avoid`)) {
        queryParams['avoid'] <- avoid
      }

      if (!missing(`key`)) {
        queryParams['key'] <- key
      }

      urlPath <- "/route"
      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- RouteResponse$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    }
  )
)
