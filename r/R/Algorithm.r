# GraphHopper Directions API
# 
# You use the GraphHopper Directions API to add route planning, navigation and route optimization to your software. E.g. the Routing API has turn instructions and elevation data and the Route Optimization API solves your logistic problems and supports various constraints like time window and capacity restrictions. Also it is possible to get all distances between all locations with our fast Matrix API.
# 
# OpenAPI spec version: 1.0.0
# 
# Generated by: https://github.com/swagger-api/swagger-codegen.git


#' Algorithm Class
#'
#' @field problem_type 
#' @field objective 
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
Algorithm <- R6::R6Class(
  'Algorithm',
  public = list(
    `problem_type` = NULL,
    `objective` = NULL,
    initialize = function(`problem_type`, `objective`){
      if (!missing(`problem_type`)) {
        stopifnot(is.character(`problem_type`), length(`problem_type`) == 1)
        self$`problem_type` <- `problem_type`
      }
      if (!missing(`objective`)) {
        stopifnot(is.character(`objective`), length(`objective`) == 1)
        self$`objective` <- `objective`
      }
    },
    toJSON = function() {
      AlgorithmObject <- list()
      if (!is.null(self$`problem_type`)) {
        AlgorithmObject[['problem_type']] <- self$`problem_type`
      }
      if (!is.null(self$`objective`)) {
        AlgorithmObject[['objective']] <- self$`objective`
      }

      AlgorithmObject
    },
    fromJSON = function(AlgorithmJson) {
      AlgorithmObject <- jsonlite::fromJSON(AlgorithmJson)
      if (!is.null(AlgorithmObject$`problem_type`)) {
        self$`problem_type` <- AlgorithmObject$`problem_type`
      }
      if (!is.null(AlgorithmObject$`objective`)) {
        self$`objective` <- AlgorithmObject$`objective`
      }
    },
    toJSONString = function() {
       sprintf(
        '{
           "problem_type": %s,
           "objective": %s
        }',
        self$`problem_type`,
        self$`objective`
      )
    },
    fromJSONString = function(AlgorithmJson) {
      AlgorithmObject <- jsonlite::fromJSON(AlgorithmJson)
      self$`problem_type` <- AlgorithmObject$`problem_type`
      self$`objective` <- AlgorithmObject$`objective`
    }
  )
)
