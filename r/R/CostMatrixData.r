# GraphHopper Directions API
# 
# You use the GraphHopper Directions API to add route planning, navigation and route optimization to your software. E.g. the Routing API has turn instructions and elevation data and the Route Optimization API solves your logistic problems and supports various constraints like time window and capacity restrictions. Also it is possible to get all distances between all locations with our fast Matrix API.
# 
# OpenAPI spec version: 1.0.0
# 
# Generated by: https://github.com/swagger-api/swagger-codegen.git


#' CostMatrixData Class
#'
#' @field times 
#' @field distances 
#' @field info 
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
CostMatrixData <- R6::R6Class(
  'CostMatrixData',
  public = list(
    `times` = NULL,
    `distances` = NULL,
    `info` = NULL,
    initialize = function(`times`, `distances`, `info`){
      if (!missing(`times`)) {
        stopifnot(is.list(`times`), length(`times`) != 0)
        lapply(`times`, function(x) stopifnot(R6::is.R6(x)))
        self$`times` <- `times`
      }
      if (!missing(`distances`)) {
        stopifnot(is.list(`distances`), length(`distances`) != 0)
        lapply(`distances`, function(x) stopifnot(R6::is.R6(x)))
        self$`distances` <- `distances`
      }
      if (!missing(`info`)) {
        stopifnot(R6::is.R6(`info`))
        self$`info` <- `info`
      }
    },
    toJSON = function() {
      CostMatrixDataObject <- list()
      if (!is.null(self$`times`)) {
        CostMatrixDataObject[['times']] <- lapply(self$`times`, function(x) x$toJSON())
      }
      if (!is.null(self$`distances`)) {
        CostMatrixDataObject[['distances']] <- lapply(self$`distances`, function(x) x$toJSON())
      }
      if (!is.null(self$`info`)) {
        CostMatrixDataObject[['info']] <- self$`info`$toJSON()
      }

      CostMatrixDataObject
    },
    fromJSON = function(CostMatrixDataJson) {
      CostMatrixDataObject <- jsonlite::fromJSON(CostMatrixDataJson)
      if (!is.null(CostMatrixDataObject$`times`)) {
        self$`times` <- lapply(CostMatrixDataObject$`times`, function(x) {
          timesObject <- Integer$new()
          timesObject$fromJSON(jsonlite::toJSON(x, auto_unbox = TRUE))
          timesObject
        })
      }
      if (!is.null(CostMatrixDataObject$`distances`)) {
        self$`distances` <- lapply(CostMatrixDataObject$`distances`, function(x) {
          distancesObject <- Numeric$new()
          distancesObject$fromJSON(jsonlite::toJSON(x, auto_unbox = TRUE))
          distancesObject
        })
      }
      if (!is.null(CostMatrixDataObject$`info`)) {
        infoObject <- CostMatrixDataInfo$new()
        infoObject$fromJSON(jsonlite::toJSON(CostMatrixDataObject$info, auto_unbox = TRUE))
        self$`info` <- infoObject
      }
    },
    toJSONString = function() {
       sprintf(
        '{
           "times": [%s],
           "distances": [%s],
           "info": %s
        }',
        lapply(self$`times`, function(x) paste(x$toJSON(), sep=",")),
        lapply(self$`distances`, function(x) paste(x$toJSON(), sep=",")),
        self$`info`$toJSON()
      )
    },
    fromJSONString = function(CostMatrixDataJson) {
      CostMatrixDataObject <- jsonlite::fromJSON(CostMatrixDataJson)
      self$`times` <- lapply(CostMatrixDataObject$`times`, function(x) Integer$new()$fromJSON(jsonlite::toJSON(x, auto_unbox = TRUE)))
      self$`distances` <- lapply(CostMatrixDataObject$`distances`, function(x) Numeric$new()$fromJSON(jsonlite::toJSON(x, auto_unbox = TRUE)))
      CostMatrixDataInfoObject -> CostMatrixDataInfo$new()
      self$`info` <- CostMatrixDataInfoObject$fromJSON(jsonlite::toJSON(CostMatrixDataObject$info, auto_unbox = TRUE))
    }
  )
)
