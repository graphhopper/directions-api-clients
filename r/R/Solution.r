# GraphHopper Directions API
# 
# You use the GraphHopper Directions API to add route planning, navigation and route optimization to your software. E.g. the Routing API has turn instructions and elevation data and the Route Optimization API solves your logistic problems and supports various constraints like time window and capacity restrictions. Also it is possible to get all distances between all locations with our fast Matrix API.
# 
# OpenAPI spec version: 1.0.0
# 
# Generated by: https://github.com/swagger-api/swagger-codegen.git


#' Solution Class
#'
#' @field costs 
#' @field distance 
#' @field time 
#' @field transport_time 
#' @field max_operation_time 
#' @field waiting_time 
#' @field no_vehicles 
#' @field no_unassigned 
#' @field routes 
#' @field unassigned 
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
Solution <- R6::R6Class(
  'Solution',
  public = list(
    `costs` = NULL,
    `distance` = NULL,
    `time` = NULL,
    `transport_time` = NULL,
    `max_operation_time` = NULL,
    `waiting_time` = NULL,
    `no_vehicles` = NULL,
    `no_unassigned` = NULL,
    `routes` = NULL,
    `unassigned` = NULL,
    initialize = function(`costs`, `distance`, `time`, `transport_time`, `max_operation_time`, `waiting_time`, `no_vehicles`, `no_unassigned`, `routes`, `unassigned`){
      if (!missing(`costs`)) {
        stopifnot(is.numeric(`costs`), length(`costs`) == 1)
        self$`costs` <- `costs`
      }
      if (!missing(`distance`)) {
        stopifnot(is.numeric(`distance`), length(`distance`) == 1)
        self$`distance` <- `distance`
      }
      if (!missing(`time`)) {
        stopifnot(is.numeric(`time`), length(`time`) == 1)
        self$`time` <- `time`
      }
      if (!missing(`transport_time`)) {
        stopifnot(is.numeric(`transport_time`), length(`transport_time`) == 1)
        self$`transport_time` <- `transport_time`
      }
      if (!missing(`max_operation_time`)) {
        stopifnot(is.numeric(`max_operation_time`), length(`max_operation_time`) == 1)
        self$`max_operation_time` <- `max_operation_time`
      }
      if (!missing(`waiting_time`)) {
        stopifnot(is.numeric(`waiting_time`), length(`waiting_time`) == 1)
        self$`waiting_time` <- `waiting_time`
      }
      if (!missing(`no_vehicles`)) {
        stopifnot(is.numeric(`no_vehicles`), length(`no_vehicles`) == 1)
        self$`no_vehicles` <- `no_vehicles`
      }
      if (!missing(`no_unassigned`)) {
        stopifnot(is.numeric(`no_unassigned`), length(`no_unassigned`) == 1)
        self$`no_unassigned` <- `no_unassigned`
      }
      if (!missing(`routes`)) {
        stopifnot(is.list(`routes`), length(`routes`) != 0)
        lapply(`routes`, function(x) stopifnot(R6::is.R6(x)))
        self$`routes` <- `routes`
      }
      if (!missing(`unassigned`)) {
        stopifnot(R6::is.R6(`unassigned`))
        self$`unassigned` <- `unassigned`
      }
    },
    toJSON = function() {
      SolutionObject <- list()
      if (!is.null(self$`costs`)) {
        SolutionObject[['costs']] <- self$`costs`
      }
      if (!is.null(self$`distance`)) {
        SolutionObject[['distance']] <- self$`distance`
      }
      if (!is.null(self$`time`)) {
        SolutionObject[['time']] <- self$`time`
      }
      if (!is.null(self$`transport_time`)) {
        SolutionObject[['transport_time']] <- self$`transport_time`
      }
      if (!is.null(self$`max_operation_time`)) {
        SolutionObject[['max_operation_time']] <- self$`max_operation_time`
      }
      if (!is.null(self$`waiting_time`)) {
        SolutionObject[['waiting_time']] <- self$`waiting_time`
      }
      if (!is.null(self$`no_vehicles`)) {
        SolutionObject[['no_vehicles']] <- self$`no_vehicles`
      }
      if (!is.null(self$`no_unassigned`)) {
        SolutionObject[['no_unassigned']] <- self$`no_unassigned`
      }
      if (!is.null(self$`routes`)) {
        SolutionObject[['routes']] <- lapply(self$`routes`, function(x) x$toJSON())
      }
      if (!is.null(self$`unassigned`)) {
        SolutionObject[['unassigned']] <- self$`unassigned`$toJSON()
      }

      SolutionObject
    },
    fromJSON = function(SolutionJson) {
      SolutionObject <- jsonlite::fromJSON(SolutionJson)
      if (!is.null(SolutionObject$`costs`)) {
        self$`costs` <- SolutionObject$`costs`
      }
      if (!is.null(SolutionObject$`distance`)) {
        self$`distance` <- SolutionObject$`distance`
      }
      if (!is.null(SolutionObject$`time`)) {
        self$`time` <- SolutionObject$`time`
      }
      if (!is.null(SolutionObject$`transport_time`)) {
        self$`transport_time` <- SolutionObject$`transport_time`
      }
      if (!is.null(SolutionObject$`max_operation_time`)) {
        self$`max_operation_time` <- SolutionObject$`max_operation_time`
      }
      if (!is.null(SolutionObject$`waiting_time`)) {
        self$`waiting_time` <- SolutionObject$`waiting_time`
      }
      if (!is.null(SolutionObject$`no_vehicles`)) {
        self$`no_vehicles` <- SolutionObject$`no_vehicles`
      }
      if (!is.null(SolutionObject$`no_unassigned`)) {
        self$`no_unassigned` <- SolutionObject$`no_unassigned`
      }
      if (!is.null(SolutionObject$`routes`)) {
        self$`routes` <- lapply(SolutionObject$`routes`, function(x) {
          routesObject <- Route$new()
          routesObject$fromJSON(jsonlite::toJSON(x, auto_unbox = TRUE))
          routesObject
        })
      }
      if (!is.null(SolutionObject$`unassigned`)) {
        unassignedObject <- SolutionUnassigned$new()
        unassignedObject$fromJSON(jsonlite::toJSON(SolutionObject$unassigned, auto_unbox = TRUE))
        self$`unassigned` <- unassignedObject
      }
    },
    toJSONString = function() {
       sprintf(
        '{
           "costs": %s,
           "distance": %s,
           "time": %d,
           "transport_time": %d,
           "max_operation_time": %d,
           "waiting_time": %d,
           "no_vehicles": %s,
           "no_unassigned": %s,
           "routes": [%s],
           "unassigned": %s
        }',
        self$`costs`,
        self$`distance`,
        self$`time`,
        self$`transport_time`,
        self$`max_operation_time`,
        self$`waiting_time`,
        self$`no_vehicles`,
        self$`no_unassigned`,
        lapply(self$`routes`, function(x) paste(x$toJSON(), sep=",")),
        self$`unassigned`$toJSON()
      )
    },
    fromJSONString = function(SolutionJson) {
      SolutionObject <- jsonlite::fromJSON(SolutionJson)
      self$`costs` <- SolutionObject$`costs`
      self$`distance` <- SolutionObject$`distance`
      self$`time` <- SolutionObject$`time`
      self$`transport_time` <- SolutionObject$`transport_time`
      self$`max_operation_time` <- SolutionObject$`max_operation_time`
      self$`waiting_time` <- SolutionObject$`waiting_time`
      self$`no_vehicles` <- SolutionObject$`no_vehicles`
      self$`no_unassigned` <- SolutionObject$`no_unassigned`
      self$`routes` <- lapply(SolutionObject$`routes`, function(x) Route$new()$fromJSON(jsonlite::toJSON(x, auto_unbox = TRUE)))
      SolutionUnassignedObject -> SolutionUnassigned$new()
      self$`unassigned` <- SolutionUnassignedObject$fromJSON(jsonlite::toJSON(SolutionObject$unassigned, auto_unbox = TRUE))
    }
  )
)
