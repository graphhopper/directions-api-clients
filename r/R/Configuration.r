# GraphHopper Directions API
#
# You use the GraphHopper Directions API to add route planning, navigation and route optimization to your software. E.g. the Routing API has turn instructions and elevation data and the Route Optimization API solves your logistic problems and supports various constraints like time window and capacity restrictions. Also it is possible to get all distances between all locations with our fast Matrix API.
#
# OpenAPI spec version: 1.0.0
# 
# Generated by: https://openapi-generator.tech


#' Configuration Class
#'
#' @field routing 
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
Configuration <- R6::R6Class(
  'Configuration',
  public = list(
    `routing` = NULL,
    initialize = function(`routing`){
      if (!missing(`routing`)) {
        stopifnot(R6::is.R6(`routing`))
        self$`routing` <- `routing`
      }
    },
    toJSON = function() {
      ConfigurationObject <- list()
      if (!is.null(self$`routing`)) {
        ConfigurationObject[['routing']] <- self$`routing`$toJSON()
      }

      ConfigurationObject
    },
    fromJSON = function(ConfigurationJson) {
      ConfigurationObject <- jsonlite::fromJSON(ConfigurationJson)
      if (!is.null(ConfigurationObject$`routing`)) {
        routingObject <- Routing$new()
        routingObject$fromJSON(jsonlite::toJSON(ConfigurationObject$routing, auto_unbox = TRUE))
        self$`routing` <- routingObject
      }
    },
    toJSONString = function() {
       sprintf(
        '{
           "routing": %s
        }',
        self$`routing`$toJSON()
      )
    },
    fromJSONString = function(ConfigurationJson) {
      ConfigurationObject <- jsonlite::fromJSON(ConfigurationJson)
      RoutingObject <- Routing$new()
      self$`routing` <- RoutingObject$fromJSON(jsonlite::toJSON(ConfigurationObject$routing, auto_unbox = TRUE))
    }
  )
)
