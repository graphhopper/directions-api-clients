/* 
 * GraphHopper Directions API
 *
 * You use the GraphHopper Directions API to add route planning, navigation and route optimization to your software. E.g. the Routing API has turn instructions and elevation data and the Route Optimization API solves your logistic problems and supports various constraints like time window and capacity restrictions. Also it is possible to get all distances between all locations with our fast Matrix API.
 *
 * OpenAPI spec version: 1.0.0
 * 
 * Generated by: https://github.com/swagger-api/swagger-codegen.git
 */

use std::rc::Rc;
use std::borrow::Borrow;
use std::borrow::Cow;

use hyper;
use serde_json;
use futures;
use futures::{Future, Stream};

use hyper::header::UserAgent;

use super::{Error, configuration};

pub struct IsochroneApiClient<C: hyper::client::Connect> {
    configuration: Rc<configuration::Configuration<C>>,
}

impl<C: hyper::client::Connect> IsochroneApiClient<C> {
    pub fn new(configuration: Rc<configuration::Configuration<C>>) -> IsochroneApiClient<C> {
        IsochroneApiClient {
            configuration: configuration,
        }
    }
}

pub trait IsochroneApi {
    fn isochrone_get(&self, point: &str, key: &str, time_limit: i32, distance_limit: i32, vehicle: &str, buckets: i32, reverse_flow: bool, weighting: &str) -> Box<Future<Item = ::models::IsochroneResponse, Error = Error<serde_json::Value>>>;
}


impl<C: hyper::client::Connect>IsochroneApi for IsochroneApiClient<C> {
    fn isochrone_get(&self, point: &str, key: &str, time_limit: i32, distance_limit: i32, vehicle: &str, buckets: i32, reverse_flow: bool, weighting: &str) -> Box<Future<Item = ::models::IsochroneResponse, Error = Error<serde_json::Value>>> {
        let configuration: &configuration::Configuration<C> = self.configuration.borrow();

        let method = hyper::Method::Get;

        let query = ::url::form_urlencoded::Serializer::new(String::new())
            .append_pair("point", &point.to_string())
            .append_pair("time_limit", &time_limit.to_string())
            .append_pair("distance_limit", &distance_limit.to_string())
            .append_pair("vehicle", &vehicle.to_string())
            .append_pair("buckets", &buckets.to_string())
            .append_pair("reverse_flow", &reverse_flow.to_string())
            .append_pair("weighting", &weighting.to_string())
            .append_pair("key", &key.to_string())
            .finish();
        let uri_str = format!("{}/isochrone{}", configuration.base_path, query);

        let uri = uri_str.parse();
        // TODO(farcaller): handle error
        // if let Err(e) = uri {
        //     return Box::new(futures::future::err(e));
        // }
        let mut req = hyper::Request::new(method, uri.unwrap());

        if let Some(ref user_agent) = configuration.user_agent {
            req.headers_mut().set(UserAgent::new(Cow::Owned(user_agent.clone())));
        }



        // send request
        Box::new(
        configuration.client.request(req)
            .map_err(|e| Error::from(e))
            .and_then(|resp| {
                let status = resp.status();
                resp.body().concat2()
                    .and_then(move |body| Ok((status, body)))
                    .map_err(|e| Error::from(e))
            })
            .and_then(|(status, body)| {
                if status.is_success() {
                    Ok(body)
                } else {
                    Err(Error::from((status, &*body)))
                }
            })
            .and_then(|body| {
                let parsed: Result<::models::IsochroneResponse, _> = serde_json::from_slice(&body);
                parsed.map_err(|e| Error::from(e))
            })
        )
    }

}
