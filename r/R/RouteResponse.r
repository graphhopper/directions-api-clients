# GraphHopper Directions API
#
# You use the GraphHopper Directions API to add route planning, navigation and route optimization to your software. E.g. the Routing API has turn instructions and elevation data and the Route Optimization API solves your logistic problems and supports various constraints like time window and capacity restrictions. Also it is possible to get all distances between all locations with our fast Matrix API.
#
# OpenAPI spec version: 1.0.0
# 
# Generated by: https://openapi-generator.tech


#' RouteResponse Class
#'
#' @field paths 
#' @field info 
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
RouteResponse <- R6::R6Class(
  'RouteResponse',
  public = list(
    `paths` = NULL,
    `info` = NULL,
    initialize = function(`paths`, `info`){
      if (!missing(`paths`)) {
        stopifnot(is.list(`paths`), length(`paths`) != 0)
        lapply(`paths`, function(x) stopifnot(R6::is.R6(x)))
        self$`paths` <- `paths`
      }
      if (!missing(`info`)) {
        stopifnot(R6::is.R6(`info`))
        self$`info` <- `info`
      }
    },
    toJSON = function() {
      RouteResponseObject <- list()
      if (!is.null(self$`paths`)) {
        RouteResponseObject[['paths']] <- lapply(self$`paths`, function(x) x$toJSON())
      }
      if (!is.null(self$`info`)) {
        RouteResponseObject[['info']] <- self$`info`$toJSON()
      }

      RouteResponseObject
    },
    fromJSON = function(RouteResponseJson) {
      RouteResponseObject <- jsonlite::fromJSON(RouteResponseJson)
      if (!is.null(RouteResponseObject$`paths`)) {
        self$`paths` <- lapply(RouteResponseObject$`paths`, function(x) {
          pathsObject <- RouteResponsePath$new()
          pathsObject$fromJSON(jsonlite::toJSON(x, auto_unbox = TRUE))
          pathsObject
        })
      }
      if (!is.null(RouteResponseObject$`info`)) {
        infoObject <- ResponseInfo$new()
        infoObject$fromJSON(jsonlite::toJSON(RouteResponseObject$info, auto_unbox = TRUE))
        self$`info` <- infoObject
      }
    },
    toJSONString = function() {
       sprintf(
        '{
           "paths": [%s],
           "info": %s
        }',
        lapply(self$`paths`, function(x) paste(x$toJSON(), sep=",")),
        self$`info`$toJSON()
      )
    },
    fromJSONString = function(RouteResponseJson) {
      RouteResponseObject <- jsonlite::fromJSON(RouteResponseJson)
      self$`paths` <- lapply(RouteResponseObject$`paths`, function(x) RouteResponsePath$new()$fromJSON(jsonlite::toJSON(x, auto_unbox = TRUE)))
      ResponseInfoObject <- ResponseInfo$new()
      self$`info` <- ResponseInfoObject$fromJSON(jsonlite::toJSON(RouteResponseObject$info, auto_unbox = TRUE))
    }
  )
)
