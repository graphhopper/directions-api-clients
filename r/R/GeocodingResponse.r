# GraphHopper Directions API
# 
# You use the GraphHopper Directions API to add route planning, navigation and route optimization to your software. E.g. the Routing API has turn instructions and elevation data and the Route Optimization API solves your logistic problems and supports various constraints like time window and capacity restrictions. Also it is possible to get all distances between all locations with our fast Matrix API.
# 
# OpenAPI spec version: 1.0.0
# 
# Generated by: https://github.com/swagger-api/swagger-codegen.git


#' GeocodingResponse Class
#'
#' @field hits 
#' @field locale 
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
GeocodingResponse <- R6::R6Class(
  'GeocodingResponse',
  public = list(
    `hits` = NULL,
    `locale` = NULL,
    initialize = function(`hits`, `locale`){
      if (!missing(`hits`)) {
        stopifnot(is.list(`hits`), length(`hits`) != 0)
        lapply(`hits`, function(x) stopifnot(R6::is.R6(x)))
        self$`hits` <- `hits`
      }
      if (!missing(`locale`)) {
        stopifnot(is.character(`locale`), length(`locale`) == 1)
        self$`locale` <- `locale`
      }
    },
    toJSON = function() {
      GeocodingResponseObject <- list()
      if (!is.null(self$`hits`)) {
        GeocodingResponseObject[['hits']] <- lapply(self$`hits`, function(x) x$toJSON())
      }
      if (!is.null(self$`locale`)) {
        GeocodingResponseObject[['locale']] <- self$`locale`
      }

      GeocodingResponseObject
    },
    fromJSON = function(GeocodingResponseJson) {
      GeocodingResponseObject <- jsonlite::fromJSON(GeocodingResponseJson)
      if (!is.null(GeocodingResponseObject$`hits`)) {
        self$`hits` <- lapply(GeocodingResponseObject$`hits`, function(x) {
          hitsObject <- GeocodingLocation$new()
          hitsObject$fromJSON(jsonlite::toJSON(x, auto_unbox = TRUE))
          hitsObject
        })
      }
      if (!is.null(GeocodingResponseObject$`locale`)) {
        self$`locale` <- GeocodingResponseObject$`locale`
      }
    },
    toJSONString = function() {
       sprintf(
        '{
           "hits": [%s],
           "locale": %s
        }',
        lapply(self$`hits`, function(x) paste(x$toJSON(), sep=",")),
        self$`locale`
      )
    },
    fromJSONString = function(GeocodingResponseJson) {
      GeocodingResponseObject <- jsonlite::fromJSON(GeocodingResponseJson)
      self$`hits` <- lapply(GeocodingResponseObject$`hits`, function(x) GeocodingLocation$new()$fromJSON(jsonlite::toJSON(x, auto_unbox = TRUE)))
      self$`locale` <- GeocodingResponseObject$`locale`
    }
  )
)
